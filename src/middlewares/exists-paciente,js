 const { getPatient } = require("../database/queries/paciente.queries");

 const existsPaciente = async (username) =>{
   const paciente = await getPatient(username);
   if(!paciente){
     throw new Error('Pciente no existe');
   }
 }

module.exports = {
  existsPaciente
}